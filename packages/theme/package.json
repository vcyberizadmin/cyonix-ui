{
  "name": "@cyonix/theme",
  "version": "3.1.0",
  "description": "Shared Tailwind v4 design tokens for the Cyonix apps",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": "./theme.css",
    "./theme.css": "./theme.css"
  },
  "files": [
    "theme.css"
  ],
  "sideEffects": [
    "*.css"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "echo \"@cyonix/theme is published as-is (no build step)\" && exit 0",
    "verify:tokens": "node scripts/verify-tokens.mjs",
    "test": "pnpm verify:tokens && pnpm typecheck && pnpm test:unit",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "vitest": "^4.1.11"
  }
}
